<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <!--
          MirrorValidation reads the real, live injazedu_lab database on
          purpose (it validates the actual imported mirror) and is never run
          by `composer test` / `php artisan test` without an explicit
          testsuite flag naming it — see composer.json's "test:mirror" script.
        -->
        <testsuite name="MirrorValidation">
            <directory>tests/Validation</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
          DB_* is intentionally NOT overridden here. .env.testing (loaded by
          APP_ENV=testing above) fully specifies the pgsql connection against
          the dedicated, disposable injazedu_lab_test database. Setting any
          DB_* var in this block would shadow .env.testing (Dotenv will not
          overwrite an already set environment variable), which is exactly
          the gap that once let tests fall through to the real injazedu_lab.
        -->
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
